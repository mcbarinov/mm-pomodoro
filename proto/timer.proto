syntax = "proto3";
package timer;

service TimerService {
  rpc Status (Empty) returns (TimerStatus);
  rpc Pause (Empty) returns (TimerStatus);
  rpc Resume (Empty) returns (TimerStatus);
  rpc Stop (Empty) returns (TimerStatus);
}

message Empty {

}

message TimerStatus {
  string status = 1;
}